<h4>Categorias: </h4>
<div class="card">
    <div class="card-bold">
{{#each categorias}}
    <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
{{else}}
    <h5>Nenhuma categoria registrada</h5>

{{/each}}
    </div>
</div>